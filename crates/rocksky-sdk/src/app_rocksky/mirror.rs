// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: app.rocksky.mirror.defs
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

//! Generated bindings for the `app.rocksky.mirror` Lexicon namespace/module.
pub mod get_mirror_sources;
pub mod put_mirror_source;

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::Datetime;
use jacquard_common::types::value::Data;
use jacquard_derive::IntoStatic;
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct MirrorSourceView<S: BosStr = DefaultStr> {
    ///Whether scrobbles from this source are being mirrored into Rocksky.
    pub enabled: bool,
    ///Username on the external service (Last.fm / ListenBrainz). Null for Teal.fm.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub external_username: Option<S>,
    ///True when an API key is stored. Last.fm/ListenBrainz only; always false for Teal.fm.
    pub has_credentials: bool,
    ///The last time the mirror process successfully polled this source.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub last_polled_at: Option<Datetime>,
    ///Watermark — scrobbles from the external service older than this are skipped.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub last_scrobble_seen_at: Option<Datetime>,
    ///One of: lastfm, listenbrainz, tealfm
    pub provider: S,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

impl<S: BosStr> LexiconSchema for MirrorSourceView<S> {
    fn nsid() -> &'static str {
        "app.rocksky.mirror.defs"
    }
    fn def_name() -> &'static str {
        "mirrorSourceView"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_app_rocksky_mirror_defs()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        Ok(())
    }
}

pub mod mirror_source_view_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Enabled;
        type HasCredentials;
        type Provider;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Enabled = Unset;
        type HasCredentials = Unset;
        type Provider = Unset;
    }
    ///State transition - sets the `enabled` field to Set
    pub struct SetEnabled<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetEnabled<St> {}
    impl<St: State> State for SetEnabled<St> {
        type Enabled = Set<members::enabled>;
        type HasCredentials = St::HasCredentials;
        type Provider = St::Provider;
    }
    ///State transition - sets the `has_credentials` field to Set
    pub struct SetHasCredentials<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetHasCredentials<St> {}
    impl<St: State> State for SetHasCredentials<St> {
        type Enabled = St::Enabled;
        type HasCredentials = Set<members::has_credentials>;
        type Provider = St::Provider;
    }
    ///State transition - sets the `provider` field to Set
    pub struct SetProvider<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetProvider<St> {}
    impl<St: State> State for SetProvider<St> {
        type Enabled = St::Enabled;
        type HasCredentials = St::HasCredentials;
        type Provider = Set<members::provider>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `enabled` field
        pub struct enabled(());
        ///Marker type for the `has_credentials` field
        pub struct has_credentials(());
        ///Marker type for the `provider` field
        pub struct provider(());
    }
}

/// Builder for constructing an instance of this type.
pub struct MirrorSourceViewBuilder<St: mirror_source_view_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (
        Option<bool>,
        Option<S>,
        Option<bool>,
        Option<Datetime>,
        Option<Datetime>,
        Option<S>,
    ),
    _type: PhantomData<fn() -> S>,
}

impl MirrorSourceView<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> MirrorSourceViewBuilder<mirror_source_view_state::Empty, DefaultStr> {
        MirrorSourceViewBuilder::new()
    }
}

impl<S: BosStr> MirrorSourceView<S> {
    /// Create a new builder for this type
    pub fn builder() -> MirrorSourceViewBuilder<mirror_source_view_state::Empty, S> {
        MirrorSourceViewBuilder::builder()
    }
}

impl MirrorSourceViewBuilder<mirror_source_view_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        MirrorSourceViewBuilder {
            _state: PhantomData,
            _fields: (None, None, None, None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> MirrorSourceViewBuilder<mirror_source_view_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        MirrorSourceViewBuilder {
            _state: PhantomData,
            _fields: (None, None, None, None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> MirrorSourceViewBuilder<St, S>
where
    St: mirror_source_view_state::State,
    St::Enabled: mirror_source_view_state::IsUnset,
{
    /// Set the `enabled` field (required)
    pub fn enabled(
        mut self,
        value: impl Into<bool>,
    ) -> MirrorSourceViewBuilder<mirror_source_view_state::SetEnabled<St>, S> {
        self._fields.0 = Option::Some(value.into());
        MirrorSourceViewBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St: mirror_source_view_state::State, S: BosStr> MirrorSourceViewBuilder<St, S> {
    /// Set the `externalUsername` field (optional)
    pub fn external_username(mut self, value: impl Into<Option<S>>) -> Self {
        self._fields.1 = value.into();
        self
    }
    /// Set the `externalUsername` field to an Option value (optional)
    pub fn maybe_external_username(mut self, value: Option<S>) -> Self {
        self._fields.1 = value;
        self
    }
}

impl<St, S: BosStr> MirrorSourceViewBuilder<St, S>
where
    St: mirror_source_view_state::State,
    St::HasCredentials: mirror_source_view_state::IsUnset,
{
    /// Set the `hasCredentials` field (required)
    pub fn has_credentials(
        mut self,
        value: impl Into<bool>,
    ) -> MirrorSourceViewBuilder<mirror_source_view_state::SetHasCredentials<St>, S> {
        self._fields.2 = Option::Some(value.into());
        MirrorSourceViewBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St: mirror_source_view_state::State, S: BosStr> MirrorSourceViewBuilder<St, S> {
    /// Set the `lastPolledAt` field (optional)
    pub fn last_polled_at(mut self, value: impl Into<Option<Datetime>>) -> Self {
        self._fields.3 = value.into();
        self
    }
    /// Set the `lastPolledAt` field to an Option value (optional)
    pub fn maybe_last_polled_at(mut self, value: Option<Datetime>) -> Self {
        self._fields.3 = value;
        self
    }
}

impl<St: mirror_source_view_state::State, S: BosStr> MirrorSourceViewBuilder<St, S> {
    /// Set the `lastScrobbleSeenAt` field (optional)
    pub fn last_scrobble_seen_at(mut self, value: impl Into<Option<Datetime>>) -> Self {
        self._fields.4 = value.into();
        self
    }
    /// Set the `lastScrobbleSeenAt` field to an Option value (optional)
    pub fn maybe_last_scrobble_seen_at(mut self, value: Option<Datetime>) -> Self {
        self._fields.4 = value;
        self
    }
}

impl<St, S: BosStr> MirrorSourceViewBuilder<St, S>
where
    St: mirror_source_view_state::State,
    St::Provider: mirror_source_view_state::IsUnset,
{
    /// Set the `provider` field (required)
    pub fn provider(
        mut self,
        value: impl Into<S>,
    ) -> MirrorSourceViewBuilder<mirror_source_view_state::SetProvider<St>, S> {
        self._fields.5 = Option::Some(value.into());
        MirrorSourceViewBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> MirrorSourceViewBuilder<St, S>
where
    St: mirror_source_view_state::State,
    St::Enabled: mirror_source_view_state::IsSet,
    St::HasCredentials: mirror_source_view_state::IsSet,
    St::Provider: mirror_source_view_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> MirrorSourceView<S> {
        MirrorSourceView {
            enabled: self._fields.0.unwrap(),
            external_username: self._fields.1,
            has_credentials: self._fields.2.unwrap(),
            last_polled_at: self._fields.3,
            last_scrobble_seen_at: self._fields.4,
            provider: self._fields.5.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> MirrorSourceView<S> {
        MirrorSourceView {
            enabled: self._fields.0.unwrap(),
            external_username: self._fields.1,
            has_credentials: self._fields.2.unwrap(),
            last_polled_at: self._fields.3,
            last_scrobble_seen_at: self._fields.4,
            provider: self._fields.5.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_app_rocksky_mirror_defs() -> LexiconDoc<'static> {
    use alloc::collections::BTreeMap;
    #[allow(unused_imports)]
    use jacquard_common::{deps::smol_str::SmolStr, types::blob::MimeType, CowStr};
    use jacquard_lexicon::lexicon::*;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("app.rocksky.mirror.defs"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("mirrorSourceView"),
                LexUserType::Object(LexObject {
                    required: Some(
                        vec![
                            SmolStr::new_static("provider"),
                            SmolStr::new_static("enabled"),
                            SmolStr::new_static("hasCredentials")
                        ],
                    ),
                    properties: {
                        #[allow(unused_mut)]
                        let mut map = BTreeMap::new();
                        map.insert(
                            SmolStr::new_static("enabled"),
                            LexObjectProperty::Boolean(LexBoolean {
                                ..Default::default()
                            }),
                        );
                        map.insert(
                            SmolStr::new_static("externalUsername"),
                            LexObjectProperty::String(LexString {
                                description: Some(
                                    CowStr::new_static(
                                        "Username on the external service (Last.fm / ListenBrainz). Null for Teal.fm.",
                                    ),
                                ),
                                ..Default::default()
                            }),
                        );
                        map.insert(
                            SmolStr::new_static("hasCredentials"),
                            LexObjectProperty::Boolean(LexBoolean {
                                ..Default::default()
                            }),
                        );
                        map.insert(
                            SmolStr::new_static("lastPolledAt"),
                            LexObjectProperty::String(LexString {
                                description: Some(
                                    CowStr::new_static(
                                        "The last time the mirror process successfully polled this source.",
                                    ),
                                ),
                                format: Some(LexStringFormat::Datetime),
                                ..Default::default()
                            }),
                        );
                        map.insert(
                            SmolStr::new_static("lastScrobbleSeenAt"),
                            LexObjectProperty::String(LexString {
                                description: Some(
                                    CowStr::new_static(
                                        "Watermark — scrobbles from the external service older than this are skipped.",
                                    ),
                                ),
                                format: Some(LexStringFormat::Datetime),
                                ..Default::default()
                            }),
                        );
                        map.insert(
                            SmolStr::new_static("provider"),
                            LexObjectProperty::String(LexString {
                                description: Some(
                                    CowStr::new_static("One of: lastfm, listenbrainz, tealfm"),
                                ),
                                ..Default::default()
                            }),
                        );
                        map
                    },
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}
