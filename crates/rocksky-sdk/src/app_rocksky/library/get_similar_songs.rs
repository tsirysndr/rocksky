// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: app.rocksky.library.getSimilarSongs
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};
use jacquard_derive::IntoStatic;
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct GetSimilarSongs<S: BosStr = DefaultStr> {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub count: Option<i64>,
    pub id: S,
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic, Default)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct GetSimilarSongsOutput<S: BosStr = DefaultStr> {
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/** Response marker for the `app.rocksky.library.getSimilarSongs` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `GetSimilarSongsOutput<S>` for this endpoint.*/
pub struct GetSimilarSongsResponse;
impl jacquard_common::xrpc::XrpcResp for GetSimilarSongsResponse {
    const NSID: &'static str = "app.rocksky.library.getSimilarSongs";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = GetSimilarSongsOutput<S>;
    type Err = jacquard_common::xrpc::GenericError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for GetSimilarSongs<S> {
    const NSID: &'static str = "app.rocksky.library.getSimilarSongs";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = GetSimilarSongsResponse;
}

/** Endpoint marker for the `app.rocksky.library.getSimilarSongs` query.

Path: `/xrpc/app.rocksky.library.getSimilarSongs`. The request payload type is `GetSimilarSongs<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct GetSimilarSongsRequest;
impl jacquard_common::xrpc::XrpcEndpoint for GetSimilarSongsRequest {
    const PATH: &'static str = "/xrpc/app.rocksky.library.getSimilarSongs";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = GetSimilarSongs<S>;
    type Response = GetSimilarSongsResponse;
}

pub mod get_similar_songs_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Id;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Id = Unset;
    }
    ///State transition - sets the `id` field to Set
    pub struct SetId<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetId<St> {}
    impl<St: State> State for SetId<St> {
        type Id = Set<members::id>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `id` field
        pub struct id(());
    }
}

/// Builder for constructing an instance of this type.
pub struct GetSimilarSongsBuilder<St: get_similar_songs_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<i64>, Option<S>),
    _type: PhantomData<fn() -> S>,
}

impl GetSimilarSongs<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> GetSimilarSongsBuilder<get_similar_songs_state::Empty, DefaultStr> {
        GetSimilarSongsBuilder::new()
    }
}

impl<S: BosStr> GetSimilarSongs<S> {
    /// Create a new builder for this type
    pub fn builder() -> GetSimilarSongsBuilder<get_similar_songs_state::Empty, S> {
        GetSimilarSongsBuilder::builder()
    }
}

impl GetSimilarSongsBuilder<get_similar_songs_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        GetSimilarSongsBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> GetSimilarSongsBuilder<get_similar_songs_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        GetSimilarSongsBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St: get_similar_songs_state::State, S: BosStr> GetSimilarSongsBuilder<St, S> {
    /// Set the `count` field (optional)
    pub fn count(mut self, value: impl Into<Option<i64>>) -> Self {
        self._fields.0 = value.into();
        self
    }
    /// Set the `count` field to an Option value (optional)
    pub fn maybe_count(mut self, value: Option<i64>) -> Self {
        self._fields.0 = value;
        self
    }
}

impl<St, S: BosStr> GetSimilarSongsBuilder<St, S>
where
    St: get_similar_songs_state::State,
    St::Id: get_similar_songs_state::IsUnset,
{
    /// Set the `id` field (required)
    pub fn id(
        mut self,
        value: impl Into<S>,
    ) -> GetSimilarSongsBuilder<get_similar_songs_state::SetId<St>, S> {
        self._fields.1 = Option::Some(value.into());
        GetSimilarSongsBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetSimilarSongsBuilder<St, S>
where
    St: get_similar_songs_state::State,
    St::Id: get_similar_songs_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> GetSimilarSongs<S> {
        GetSimilarSongs {
            count: self._fields.0,
            id: self._fields.1.unwrap(),
        }
    }
}
