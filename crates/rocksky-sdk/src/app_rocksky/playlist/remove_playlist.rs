// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: app.rocksky.playlist.removePlaylist
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::AtUri;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, DefaultStr, FromStaticStr};
use jacquard_derive::IntoStatic;
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct RemovePlaylistParams<S: BosStr = DefaultStr> {
    pub uri: AtUri<S>,
}

/** Request marker for the `app.rocksky.playlist.removePlaylist` procedure.

This endpoint has no request parameters or input body; send this marker with `jacquard::Client`.*/

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic, Copy)]
pub struct RemovePlaylist;
/** Response marker for the `app.rocksky.playlist.removePlaylist` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `()` for this endpoint.*/
pub struct RemovePlaylistResponse;
impl jacquard_common::xrpc::XrpcResp for RemovePlaylistResponse {
    const NSID: &'static str = "app.rocksky.playlist.removePlaylist";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = ();
    type Err = jacquard_common::xrpc::GenericError;
}

impl jacquard_common::xrpc::XrpcRequest for RemovePlaylist {
    const NSID: &'static str = "app.rocksky.playlist.removePlaylist";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Response = RemovePlaylistResponse;
}

/** Endpoint marker for the `app.rocksky.playlist.removePlaylist` procedure.

Path: `/xrpc/app.rocksky.playlist.removePlaylist`. The request payload type is `RemovePlaylist`; use this marker with lower-level `XrpcEndpoint` APIs when you need endpoint metadata.*/
pub struct RemovePlaylistRequest;
impl jacquard_common::xrpc::XrpcEndpoint for RemovePlaylistRequest {
    const PATH: &'static str = "/xrpc/app.rocksky.playlist.removePlaylist";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Request<S: BosStr> = RemovePlaylist;
    type Response = RemovePlaylistResponse;
}

pub mod remove_playlist_params_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Uri;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Uri = Unset;
    }
    ///State transition - sets the `uri` field to Set
    pub struct SetUri<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetUri<St> {}
    impl<St: State> State for SetUri<St> {
        type Uri = Set<members::uri>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `uri` field
        pub struct uri(());
    }
}

/// Builder for constructing an instance of this type.
pub struct RemovePlaylistParamsBuilder<
    St: remove_playlist_params_state::State,
    S: BosStr = DefaultStr,
> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<AtUri<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl RemovePlaylistParams<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> RemovePlaylistParamsBuilder<remove_playlist_params_state::Empty, DefaultStr> {
        RemovePlaylistParamsBuilder::new()
    }
}

impl<S: BosStr> RemovePlaylistParams<S> {
    /// Create a new builder for this type
    pub fn builder() -> RemovePlaylistParamsBuilder<remove_playlist_params_state::Empty, S> {
        RemovePlaylistParamsBuilder::builder()
    }
}

impl RemovePlaylistParamsBuilder<remove_playlist_params_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        RemovePlaylistParamsBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> RemovePlaylistParamsBuilder<remove_playlist_params_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        RemovePlaylistParamsBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RemovePlaylistParamsBuilder<St, S>
where
    St: remove_playlist_params_state::State,
    St::Uri: remove_playlist_params_state::IsUnset,
{
    /// Set the `uri` field (required)
    pub fn uri(
        mut self,
        value: impl Into<AtUri<S>>,
    ) -> RemovePlaylistParamsBuilder<remove_playlist_params_state::SetUri<St>, S> {
        self._fields.0 = Option::Some(value.into());
        RemovePlaylistParamsBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RemovePlaylistParamsBuilder<St, S>
where
    St: remove_playlist_params_state::State,
    St::Uri: remove_playlist_params_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> RemovePlaylistParams<S> {
        RemovePlaylistParams {
            uri: self._fields.0.unwrap(),
        }
    }
}
