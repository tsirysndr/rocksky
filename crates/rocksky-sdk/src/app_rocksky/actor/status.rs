// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: app.rocksky.actor.status
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::collection::{Collection, RecordError};
use jacquard_common::types::string::{AtUri, Cid, Datetime};
use jacquard_common::types::uri::{RecordUri, UriError};
use jacquard_common::types::value::Data;
use jacquard_common::xrpc::XrpcResp;
use jacquard_derive::{lexicon, IntoStatic};
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

use crate::app_rocksky::actor::TrackView;
#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Deserialize, Serialize};
/// The current listening status of the actor. Only one can be active at a time (rkey: self).

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    rename = "app.rocksky.actor.status",
    tag = "$type",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct Status<S: BosStr = DefaultStr> {
    ///When the status expires. Defaults to startedAt plus track duration plus idle time.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub expires_at: Option<Datetime>,
    ///When the track started playing.
    pub started_at: Datetime,
    ///The track currently being played.
    pub track: TrackView<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// Typed wrapper for GetRecord response with this collection's record type.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct StatusGetRecordOutput<S: BosStr = DefaultStr> {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cid: Option<Cid<S>>,
    pub uri: AtUri<S>,
    pub value: Status<S>,
}

impl<S: BosStr> Status<S> {
    pub fn uri(uri: S) -> Result<RecordUri<S, StatusRecord>, UriError> {
        RecordUri::try_from_uri(AtUri::new(uri)?)
    }
}

/// Marker type for deserializing records from this collection.

#[derive(Debug, Serialize, Deserialize)]
pub struct StatusRecord;
impl XrpcResp for StatusRecord {
    const NSID: &'static str = "app.rocksky.actor.status";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = StatusGetRecordOutput<S>;
    type Err = RecordError;
}

impl<S: BosStr> From<StatusGetRecordOutput<S>> for Status<S> {
    fn from(output: StatusGetRecordOutput<S>) -> Self {
        output.value
    }
}

impl<S: BosStr> Collection for Status<S> {
    const NSID: &'static str = "app.rocksky.actor.status";
    type Record = StatusRecord;
}

impl Collection for StatusRecord {
    const NSID: &'static str = "app.rocksky.actor.status";
    type Record = StatusRecord;
}

impl<S: BosStr> LexiconSchema for Status<S> {
    fn nsid() -> &'static str {
        "app.rocksky.actor.status"
    }
    fn def_name() -> &'static str {
        "main"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_app_rocksky_actor_status()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        Ok(())
    }
}

pub mod status_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type StartedAt;
        type Track;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type StartedAt = Unset;
        type Track = Unset;
    }
    ///State transition - sets the `started_at` field to Set
    pub struct SetStartedAt<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetStartedAt<St> {}
    impl<St: State> State for SetStartedAt<St> {
        type StartedAt = Set<members::started_at>;
        type Track = St::Track;
    }
    ///State transition - sets the `track` field to Set
    pub struct SetTrack<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetTrack<St> {}
    impl<St: State> State for SetTrack<St> {
        type StartedAt = St::StartedAt;
        type Track = Set<members::track>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `started_at` field
        pub struct started_at(());
        ///Marker type for the `track` field
        pub struct track(());
    }
}

/// Builder for constructing an instance of this type.
pub struct StatusBuilder<St: status_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Datetime>, Option<Datetime>, Option<TrackView<S>>),
    _type: PhantomData<fn() -> S>,
}

impl Status<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> StatusBuilder<status_state::Empty, DefaultStr> {
        StatusBuilder::new()
    }
}

impl<S: BosStr> Status<S> {
    /// Create a new builder for this type
    pub fn builder() -> StatusBuilder<status_state::Empty, S> {
        StatusBuilder::builder()
    }
}

impl StatusBuilder<status_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        StatusBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> StatusBuilder<status_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        StatusBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St: status_state::State, S: BosStr> StatusBuilder<St, S> {
    /// Set the `expiresAt` field (optional)
    pub fn expires_at(mut self, value: impl Into<Option<Datetime>>) -> Self {
        self._fields.0 = value.into();
        self
    }
    /// Set the `expiresAt` field to an Option value (optional)
    pub fn maybe_expires_at(mut self, value: Option<Datetime>) -> Self {
        self._fields.0 = value;
        self
    }
}

impl<St, S: BosStr> StatusBuilder<St, S>
where
    St: status_state::State,
    St::StartedAt: status_state::IsUnset,
{
    /// Set the `startedAt` field (required)
    pub fn started_at(
        mut self,
        value: impl Into<Datetime>,
    ) -> StatusBuilder<status_state::SetStartedAt<St>, S> {
        self._fields.1 = Option::Some(value.into());
        StatusBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> StatusBuilder<St, S>
where
    St: status_state::State,
    St::Track: status_state::IsUnset,
{
    /// Set the `track` field (required)
    pub fn track(
        mut self,
        value: impl Into<TrackView<S>>,
    ) -> StatusBuilder<status_state::SetTrack<St>, S> {
        self._fields.2 = Option::Some(value.into());
        StatusBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> StatusBuilder<St, S>
where
    St: status_state::State,
    St::StartedAt: status_state::IsSet,
    St::Track: status_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Status<S> {
        Status {
            expires_at: self._fields.0,
            started_at: self._fields.1.unwrap(),
            track: self._fields.2.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> Status<S> {
        Status {
            expires_at: self._fields.0,
            started_at: self._fields.1.unwrap(),
            track: self._fields.2.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_app_rocksky_actor_status() -> LexiconDoc<'static> {
    use alloc::collections::BTreeMap;
    #[allow(unused_imports)]
    use jacquard_common::{deps::smol_str::SmolStr, types::blob::MimeType, CowStr};
    use jacquard_lexicon::lexicon::*;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("app.rocksky.actor.status"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("main"),
                LexUserType::Record(LexRecord {
                    description: Some(
                        CowStr::new_static(
                            "The current listening status of the actor. Only one can be active at a time (rkey: self).",
                        ),
                    ),
                    key: Some(CowStr::new_static("literal:self")),
                    record: LexRecordRecord::Object(LexObject {
                        required: Some(
                            vec![
                                SmolStr::new_static("track"),
                                SmolStr::new_static("startedAt")
                            ],
                        ),
                        properties: {
                            #[allow(unused_mut)]
                            let mut map = BTreeMap::new();
                            map.insert(
                                SmolStr::new_static("expiresAt"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static(
                                            "When the status expires. Defaults to startedAt plus track duration plus idle time.",
                                        ),
                                    ),
                                    format: Some(LexStringFormat::Datetime),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("startedAt"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static("When the track started playing."),
                                    ),
                                    format: Some(LexStringFormat::Datetime),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("track"),
                                LexObjectProperty::Ref(LexRef {
                                    r#ref: CowStr::new_static(
                                        "app.rocksky.actor.defs#trackView",
                                    ),
                                    ..Default::default()
                                }),
                            );
                            map
                        },
                        ..Default::default()
                    }),
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}
