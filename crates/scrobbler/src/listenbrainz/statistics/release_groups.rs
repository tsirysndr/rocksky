use anyhow::Error;

pub async fn get_top_release_groups(_user_name: &str) -> Result<(), Error> {
    Ok(())
}
pub async fn get_release_group_listeners(_user_name: &str) -> Result<(), Error> {
    Ok(())
}

pub async fn get_sitewide_release_groups() -> Result<(), Error> {
    Ok(())
}
