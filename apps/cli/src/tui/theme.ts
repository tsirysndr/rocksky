// Shared palette — mirrors the `--help` colours from ../theme.
export { ACCENT as VIOLET, SECONDARY as BLUE, PRIMARY as TEAL } from "../theme";
export { ERROR, HIGHLIGHT, LINK, MUTED, SKY } from "../theme";
