{
  "lexicon": 1,
  "id": "app.rocksky.playlist.removeTrack",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Remove a track from a playlist. Deletes the app.rocksky.playlist.song record that put it there, which only the repo that added it can do.",
      "parameters": {
        "type": "params",
        "required": [
          "uri",
          "songUri"
        ],
        "properties": {
          "uri": {
            "type": "string",
            "description": "The URI of the playlist to remove the track from",
            "format": "at-uri"
          },
          "songUri": {
            "type": "string",
            "description": "The URI of the app.rocksky.song record to remove from the playlist",
            "format": "at-uri"
          }
        }
      }
    }
  }
}
