amends "../../schema/lexicon.pkl"

lexicon = 1
id = "app.rocksky.song.getSongs"
defs = new Mapping<String, Query> {
  ["main"] {
    type = "query"
    description = "Get songs"
    parameters = new Params {
      properties {
        ["limit"] = new IntegerType {
          type = "integer"
          description = "The maximum number of songs to return"
          minimum = 1
        }
        ["offset"] = new IntegerType {
          type = "integer"
          description = "The offset for pagination"
          minimum = 0
        }
        ["genre"] = new StringType {
          type = "string"
          description = "The genre to filter artists by"
        }
        ["mbid"] = new StringType {
          type = "string"
          description = "Filter songs by MusicBrainz ID"
        }
        ["isrc"] = new StringType {
          type = "string"
          description = "Filter songs by International Standard Recording Code (ISRC)"
        }
        ["spotifyId"] = new StringType {
          type = "string"
          description = "Filter songs by Spotify track ID (resolved internally to the Spotify track URL)"
        }
        ["filter"] = new StringType {
          type = "string"
          description = "RSQL filter expression, e.g. `artist==\"Daft Punk\";duration=gt=200000`. Supports ==, !=, <, <=, >, >=, =in=, =out=, and `;`/`and`, `,`/`or` combinators, `*` wildcards in string values. Filterable fields: title, artist, album, albumArtist, genre, composer, label, duration, trackNumber, discNumber, mbId, isrc, sha256, uri, albumUri, artistUri, createdAt"
          maxLength = 2048
        }
      }
    }
    output {
      encoding = "application/json"
      schema = new ObjectType {
        type = "object"
        properties = new Mapping<String, Array> {
          ["songs"] = new Array {
            type = "array"
            items = new Ref {
              ref = "app.rocksky.song.defs#songViewBasic"
            }
          }
        }
      }
    }
  }
}
