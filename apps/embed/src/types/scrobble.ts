export type Scrobble = {
  id: string;
  trackId: string;
  title: string;
  artist: string;
  albumArtist: string;
  albumArt: string;
  cover?: string;
  album: string;
  handle: string;
  did: string;
  uri: string;
  trackUri: string;
  artistUri: string;
  albumUri: string;
  createdAt: string;
  date: string;
};
