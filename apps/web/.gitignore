# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# wrangler files
.wrangler
.dev.vars

*storybook.log

.env
.env.prod
.env.local

.tanstack
# rockbox-wasm runtime assets (copied from node_modules by vite.config.ts)
public/rockbox

# ReScript build cache (anchored to apps/web/lib — must NOT match src/lib source)
/lib/
watch.lock
.sourcedirs.json
# ReScript generated output (regenerated by `rescript build`; see pre{dev,build})
*.res.mjs
*.gen.tsx
